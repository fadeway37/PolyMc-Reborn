# Verification record: PolyMc Reborn 0.4.0 RC2

This record covers `0.4.0-rc.2+26.1.2` in the independent
`fadeway37/PolyMc-Reborn` repository. Results are accepted only for the final
RC2 commit and only after the corresponding command and structured evidence
have been inspected.

## Integrity boundary

- RC1 source baseline: `f571ac5243518387c5b1a81c818dd8ffdd8db2de`
- RC1 tree: `93e86c4e57b5486d73d0080184649fe2941b7a09`
- Production Java changes: `0`
- Mixin changes: `0`
- Access Widener changes: `0`
- Stable API breaks: `0`
- Mapping schema changes: `0`
- Mapping algorithm changes: `0`
- Behavioral default changes: `0`

RC2 changes repository identity, documentation, hosting metadata, workflow
targets, project URLs, and release-version metadata. It does not change
runtime compatibility logic.

## Gate record

The final gate matrix is populated from local command results and GitHub-hosted
artifacts after the candidate commit is fixed. Until then, an absent result is
`NOT_RUN`, not a pass.

| Layer | Final candidate result | Required inspection |
| --- | --- | --- |
| JUnit and clean build | Pending | Exit codes, XML totals, tracked-input state |
| API signature and consumers | Pending | Version-only signature delta, consumer reports |
| Fabric GameTest | Pending | Registered test totals and server exit |
| Dedicated-server smoke | Pending | Ready marker, no client linkage, clean stop |
| Single client | Pending | Scenarios, mod list, screenshots, independent observations |
| Multi-client | Pending | Scenarios, pack policy, session isolation, clean exits |
| External-mod matrix | Pending | Exact mod hashes and named feature totals |
| Upgrade and expansion | Pending | Persisted-state checks and allocation stability |
| Short and long soak | Pending | Iterations, operation floors, cleanup and resource trends |
| Cross-platform reproducibility | Pending | Windows/Linux archive names and bytes |
| Release artifacts and SBOM | Pending | Allow-list, manifests, hashes, archive contents |
| GitHub provenance | Pending | New repository, workflow, commit, subject digests, tamper rejection |

The pure zero-mod vanilla-client smoke remains `NOT_RUN` and is not a release
claim. Compatibility remains limited to the exact features and versions named
by successful evidence.
